version: 2.1

jobs:
  build:
    docker:
      - image: cimg/go:1.22.0
    steps:
      - checkout
      - run:
          name: linux AMD
          command: go build -o bogus .

workflows:
  circleci-logs-build:
    jobs:
      - build
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/go:1.18.10
    steps:
      - checkout
      - run:
          name: linux AMD
          command: go build -o bogus .

workflows:
  circleci-logs-build:
    jobs:
      - build
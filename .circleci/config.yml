version: 2.1

orbs:
  go: circleci/go@1.7.3

jobs:
  build:
    machine: # executor type
      image: prod-default
    steps:
      - checkout
      - run:
          name: linux AMD
          command: go build -o bogus .

workflows:
  circleci-logs-build:
    jobs:
      - build